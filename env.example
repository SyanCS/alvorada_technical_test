# Database Configuration
DB_HOST=db
DB_NAME=alvorada_db
DB_USER=alvorada_user
DB_PASSWORD=alvorada_password
DB_ROOT_PASSWORD=root_password

# Application Configuration
APP_ENV=development
APP_DEBUG=true

# API Configuration (for geolocation services)
# Nominatim doesn't require API key, but add your own if using Google/other services
GEOLOCATION_API_KEY=


